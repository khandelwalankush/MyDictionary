import { DataDictionaryPageContent } from '../data-dictionary-content';

export const metadata = {
  title: 'Data Dictionary | LexiField',
};

export default function DataDictionaryPage() {
  return <DataDictionaryPageContent />;
}
